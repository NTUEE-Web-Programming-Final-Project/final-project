{
  "name": "final-project",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "install:all": "cd frontend && bun install && cd .. && cd backend && bun install",
    "frontend": "cd frontend && bun run dev",
    "backend": "cd backend && pnpm start"
  }
}
