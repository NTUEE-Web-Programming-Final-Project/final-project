{
  "name": "final-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "install:all": "cd frontend && npm install --frozen-lockfile && cd ../backend && npm install --frozen-lockfile ",
    "codegen": "cd frontend && npm run codegen",
    "frontend": "cd frontend && npm run build",
    "deploy": "cd backend && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ci": "^2.3.0",
    "clsx": "^2.0.0",
    "magic-sdk": "^21.4.1"
  }
}
